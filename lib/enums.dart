enum ValueEncoding {
  number,
  text,
}

enum FieldType {
  custom,
  instrumentName,
}

enum FieldEnumeration {
  none,
  postive,
  negative,
}

enum InstrumentType {
  conventional,
  movingLight,
  atmosphericFX,
  specialFX,
  practical,
  other,
}

enum MetadataEncoding {
  text,
  number,
  instrumentType,
}

enum CellSelectionDirectionality {
  leftToRight,
  rightToLeft,
  topToBottom,
  bottomToTop,
  none,
}